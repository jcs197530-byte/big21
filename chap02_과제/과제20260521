# 파일썬의 딕셔너리 이용하여 만들기

# 1차 과제 파일썬 프로그램으로 연락처 프로그램 만들기
# 방법 1 - 클로드 코드를 이용하거나 ai 이용하여 만드는것

# 1차 과제 파일썬 프로그램으로 연락처 프로그램 만들기
# 방법 1 - 클로드 코드를 이용하거나 ai 이용하여 만드는것
# 배웠으면 만들어 봐야한다.
# 콘솔에서 진행해야 됨.

# 이름/전화번호/주소/생일 4개 항목에 대하여 
# 연락처 프로그램 진행

# 모든 과제는 팀별로 개인별 과제 제출
# 과목별 과제가 다 있다.
# 우리반은 데이터를 다루는 반이다
# 바이브코딩쪽 과제들도 많다.
# 해야 할것은 반드시 해야 한다.

# # 해당 key가 해당 딕셔너리 안에 있는지 확인 : in
# print(a)
# 'name' in a #names라는 키가  a 딕셔너리에 키로 존재하니?
# # 해당키가 있으면 True, 없으면 False 반환
# # bool 타입 :  True / False -> 파이썬에선 대문자로 시작하는 True / False로 표현한다.
# # print('names' in a)
# if 'name' in a:
#     print("name key 가 있습니다.")
# else:
#     print("name key 가 없습니다.") 


# 1. 연락처 검색할때 
# 아래 구문 사용해야 함
# # 딕셔너리 메소드 : 암기
# # 1. keys : 키를 반환한다 
# a = {
#     'name': 'pey',
#     'phone': '01012341234',
#     'birth' : '1118'
# }
# print(a)
# print(a.keys())
# print(a.values())


# 2차 웹/DB 끝나면 2차과제
# 프로그램으로 연락처 프로그램 만들기
# 웹으로 데이터베이스 연동하여 처리하는 방식
# 화면단은 바이브코딩 해도 된다